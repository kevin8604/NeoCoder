# ── Rust ──
target/
# Cargo.lock is tracked for binary reproducibility (Tauri desktop app)

# ── Node ──
node_modules/
dist/

# ── Tauri auto-generated ──
src-tauri/gen/

# ── IDE / Editor ──
.vscode/
.idea/
.qoder/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Environment / Secrets ──
.env
.env.local

# ── Logs ──
*.log

# ── Build / test artifacts ──
target188/
*_out.txt

# ── cargo-audit local advisory database clone ──
src-tauri/.audit-db/
src-tauri/.audit-db*.lock

# ── Misc ──
searxng/
TEST_REPORT.md
IssueHistory/
